
describe("foo", () => {
  it("asserts something", async () => {
    await 1;
    expect(1).toEqual(2);
  })

  it("asserts something", async () => {
    await 1;
    expect(1).toEqual(2);
  })

  it("asserts something", async () => {
    await 1;
    expect(1).toEqual(2);
  })

  it("asserts something", async () => {
    await 1;
    expect(1).toEqual(2);
  })
})
